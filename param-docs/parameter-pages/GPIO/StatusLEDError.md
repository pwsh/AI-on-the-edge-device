# Parameter `StatusLEDError`
Default Value: `120 0 0`

Colour shown when a processing step fails or a retry occurs,
in **R**ed **G**reen **B**lue from `0` (off) .. `255` (full on).
Only used when `StatusLED` is enabled.
