# Parameter `StatusLEDIdle`
Default Value: `0 10 0`

Colour shown while the device is idle / ready (waiting for the next round),
in **R**ed **G**reen **B**lue from `0` (off) .. `255` (full on).
Only used when `StatusLED` is enabled.
