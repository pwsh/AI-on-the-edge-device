# Parameter `StatusLEDDigitize`
Default Value: `80 80 0`

Colour shown while the digits/analog pointers are being recognised (CNN),
in **R**ed **G**reen **B**lue from `0` (off) .. `255` (full on).
Only used when `StatusLED` is enabled.
