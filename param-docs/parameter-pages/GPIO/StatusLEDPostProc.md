# Parameter `StatusLEDPostProc`
Default Value: `60 0 80`

Colour shown during post-processing (validation, rate checks, value assembly),
in **R**ed **G**reen **B**lue from `0` (off) .. `255` (full on).
Only used when `StatusLED` is enabled.
