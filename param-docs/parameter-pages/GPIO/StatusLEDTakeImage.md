# Parameter `StatusLEDTakeImage`
Default Value: `0 0 80`

Colour shown while a new image is being captured,
in **R**ed **G**reen **B**lue from `0` (off) .. `255` (full on).
Only used when `StatusLED` is enabled.
