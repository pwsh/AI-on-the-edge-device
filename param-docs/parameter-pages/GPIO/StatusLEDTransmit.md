# Parameter `StatusLEDTransmit`
Default Value: `0 80 80`

Colour shown while results are transmitted (MQTT, InfluxDB, Webhook),
in **R**ed **G**reen **B**lue from `0` (off) .. `255` (full on).
Only used when `StatusLED` is enabled.
