# Parameter `StatusLEDAlign`
Default Value: `90 30 0`

Colour shown while the captured image is being aligned,
in **R**ed **G**reen **B**lue from `0` (off) .. `255` (full on).
Only used when `StatusLED` is enabled.
