# 240 MHz CPU variant - native equivalent of platformio [env:esp32cam-cpu-freq-240].
# (The default ESP32-CAM build runs at the IDF default 160 MHz; the S3 build already uses 240.)
CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ_240=y
CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ=240
CONFIG_FREERTOS_HZ=1000
